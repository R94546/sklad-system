# SKLAD — Чек-лист доработок (ревью 2026-06-11)

> Цель: довести проект до профессионального уровня. Отмечай `[x]` по мере выполнения.
> Легенда приоритета: 🔴 критично (портятся данные/деньги) · 🟠 важно · 🟡 мелочь/UX.
> «✅ вживую» — подтверждено реальным запросом к API, не только по коду.

---

## 🆕 Новые задачи (от пользователя, 2026-06-12) — НАЧАТЬ ОТСЮДА

- [x] **POS оплата: ввод суммы с физической клавиатуры.** Добавлен `keydown`-слушатель в режиме оплаты (`POS.jsx`): 0-9, запятая/точка, Backspace, Enter=подтвердить, Esc=назад; не перехватывает ввод в полях. ✅
- [x] **Логирование действий пользователей.** `audit()` добавлен: PRODUCT_*, CLIENT_*, USER_*, CATEGORY_*, DEBT_PAY, INVENTORY_ADJUST (+ ранее SALE_*/STOCK_*/LOGIN). Видно в Журнале. ✅ вживую.
- [x] **Дашборд продавца: карточка «Клиенты» кликабельна** → для не-админа `navigate("/clients")`. ✅
- [x] **Товары: удаление фото при редактировании** — кнопка на превью (сброс + `imageUrl:null`, бэкенд принимает null). ✅
- [x] **Инвентаризация** — страница `/inventory` (admin, пункт меню): учётный vs факт. остаток, разница, «Применить» → `POST /products/inventory` (коррекция в транзакции + аудит `INVENTORY_ADJUST`). ✅ вживую: 14892→14899.

### ⚙️ Деплой (Vercel/Railway)
- [x] **Vercel build** — починился сам собой ещё до этой сессии (ошибки exit 127 были 17h+ назад). После `git push` (619f012) Vercel автодеплоит с GitHub: `npm install --prefix sklad-web` + `vite build` ✓ 2.38s, прод-алиас https://sklad-web-app.vercel.app отдаёт свежий бандл (Inventory-чанк на месте). ✅ вживую 2026-06-12.
- [x] **Railway (backend)** — автодеплоит с GitHub: после push'а прод уже отвечает кодом 619f012 (`POST /api/products/inventory` существует, «Инвентаризация применена»). Decimal-клиент сверен с БД: `GET /api/products` отдаёт корректные цены (12000/15000), без 500. ✅ вживую 2026-06-12. Прод-API: https://sklad-system-production.up.railway.app/api.

---

## ✅ Исправлено

- [x] **Частый разлогин** (2026-06-11) — `JWT_EXPIRES_IN` 2мин→1д + single-flight refresh (axios): при пачке параллельных 401 токен обновляется один раз, а не каждым запросом (ротация refresh-токена больше не ломается гонкой).
- [x] **Кнопка «Назад» на всех экранах** — добавлена в шапку `MainLayout` (скрыта на Панели), `navigate(-1)`.
- [x] **Закрытие модалок кликом вне** — проверено: все backdrop'ы уже закрывают окно по клику; в общий `ui/Modal` добавлено закрытие по **Escape**.
- [x] **Экран «Товары» в стиле Odoo** — kanban-карточки (название + `[код]` + цена + «В наличии») вместо таблицы + фильтр-чипы категорий.
- [x] **SaleDetail: «назад» вёл на удалённый `/sales`** → `navigate(-1)`.
- [x] **POS: задержка ~3-8 сек при добавлении товара кликом** (2026-06-11). Причина: `addToCart` ждал полный round-trip к удалённой БД (в логе POST `/sales/cart/add` = 3.3–8.0 сек). Решение: **оптимистичное обновление** чека (`cartStore.js` — товар виден мгновенно, серверный ответ сверяется в фоне, при ошибке откат через `loadCarts`) + параллельные запросы товар/корзина в бэкенде (`sales.service.js`). ⚠️ Глубинная причина медленной БД (несколько экземпляров `PrismaClient` / регион БД) остаётся в 🟠.

---

## 🧹 Структурная чистка (убрать дубли)

- [x] **Убрать «Продажи»** (дублирует «Заказы» в POS) — удалён пункт меню (`MainLayout.jsx`), роут+импорт (`App.jsx`), файл `pages/Sales.jsx`. Роут детали `/sales/:id` (`SaleDetail.jsx`) сохранён (ссылки из Дашборда/Аналитики).
- [x] **Убрать «Корзину»** (легаси, POS закрывает через параллельные чеки + «Заказы») — удалён роут+импорт (`App.jsx`), файл `pages/Cart.jsx`, индикатор корзины в шапке `MainLayout.jsx` + мёртвый `fetchCart` из `cartStore.js`. Заодно ушёл warning `MainLayout:33`.
- [x] Проверка после удаления: внешних ссылок на `/sales`(список)/`/cart` нет, `eslint` ✓ (0 проблем), `vite build` ✓.

---

## 🔴 Критично — логика денег и склада

### Продажи
- [x] **`confirmCart` не проверяет остаток** → исправлено: атомарный условный decrement (`updateMany where quantity >= qty`, проверка `count`). ✅ вживую: попытка оформить больше остатка → ошибка, склад не в минус.
- [x] **`confirmCart` не в транзакции** → обёрнут в `prisma.$transaction` (проверка остатков + списание + оформление + долг атомарно).
- [x] **Параллельные корзины не резервируют товар** → решено атомарным условным decrement: второй чек на тот же остаток получит `count===0` → ошибку и откат.
- [x] _Бонус:_ та же защита+транзакция в `kassaConfirm`; `cancel`/`remove`/`kassaReturn` — транзакция + возврат остатка только для `COMPLETED` (раньше PENDING/отменённые задваивали склад); в `remove` добавлено удаление связанного долга.

### Приход
- [x] **Нет валидации входных данных** → добавлены проверки `productId`, `qty>0` (Number.isFinite), `price>=0`, существование товара; всё в транзакции.

### Аналитика
- [x] **`getTopProducts` без фильтра статуса** → фильтр `sale.status='COMPLETED'`. ✅ вживую: «sadas» 24→23 (штука из корзины исключена).
- [x] **`getTopProfitProducts` — двойная ошибка** → фильтр `COMPLETED` + прибыль по фактической `SaleItem.price` минус закупка товара.

---

## 🟠 Важно

### Архитектура backend
- [x] **Несколько экземпляров `PrismaClient`** → `sales.service.js`, `analytics.service.js`, `stockin.routes.js` переведены на синглтон `config/db.js`.
- [x] **Узбекские сообщения об ошибках** в `sales.service.js` переведены на русский (`Товар не найден`, `Корзина не найдена`, `Продажа не найдена`, `недостаточно на складе`, и т.д.).
- [ ] **Единый слой валидации** входных данных (zod/joi) на бэке — сейчас точечно; вынести в общий слой.

### Приход
- [x] **`buyPrice` перезаписывался последней закупкой** → теперь **средневзвешенная** цена при приходе `(старый_остаток×старая_цена + приход×цена)/итог`. ✅ вживую: 50000→50009.90.
- [x] **Нет правки/удаления прихода** → добавлены `PUT/DELETE /stockin/:id` с откатом остатка (атомарно, без минуса) + UI кнопки правки/удаления в `StockIn.jsx`. ⚠️ buyPrice при правке/удалении не пересчитывается (ограничение — нельзя «отмотать» среднюю без переигрывания всех приходов).

### Товары
- [x] **Нельзя выставить 0** → `update` переписан на явные поля + хелпер `has()` (значения 0 для количества/мин.остатка теперь сохраняются).
- [x] **`create` спредит сырой `...data`** → перечислены явные поля (name/categoryId/unit/barcode/цены/количество/imageUrl).

### Аналитика
- [x] **`/products/low-stock` возвращает ВСЕ товары** → добавлен фильтр `quantity <= minStock`. ✅ вживую: теперь 1 = 1 с `/analytics/low-stock`.
- [ ] **Часовой пояс** — «сегодня/месяц» по локальному времени сервера, график по UTC-дате (`analytics.service.js`). Привести к единому TZ магазина.
- [x] **`getSellerDashboard` включает PENDING** → считает только `COMPLETED`.

### Прочее
- [x] **MIXED-оплата:** валидация в `confirmCart` — для DEBT/MIXED требуется клиент, `debtAmount > 0` и `≤ итог` (иначе откат транзакции).

---

## 🟡 Мелочь / UX

- [x] Единицы не локализованы в таблице прихода → используется карта `UNITS` (`+5 шт`).
- [x] Поиск товаров дёргает API на каждый символ → debounce 350мс (`Products.jsx`).
- [x] Нет предупреждения при `цена продажи < цены прихода` → ненавязчивое предупреждение под полями цен в форме товара.
- [x] Удаление через нативный `confirm()` → стилизованный модал подтверждения (`Products.jsx`).
- [x] График пропускает дни без продаж → нули заполняются (неделя 7 / месяц 30 дней; год — 12 точек по месяцам).
- [ ] Узбекские фоллбэки `«Noaniq»` в аналитике — `analytics.service.js:92,136`.
- [x] Access-токен жил ~120 сек → `JWT_EXPIRES_IN=1d` + single-flight refresh в axios (устранена гонка ротации refresh-токена при пачке параллельных 401, которая выкидывала из системы). ✅ вживую: токен 24ч.

---

## 🧭 Панель (Dashboard) ✅ ЗАВЕРШЕНО

Backend `getDashboard` расширен, фронт `Dashboard.jsx` — новые карточки в стиле Odoo.

- [x] **Прибыль и маржа** — карточки «Прибыль сегодня/за месяц» (факт. цена − закупка).
- [x] **Кол-во чеков + средний чек** — подпись под «Продажи сегодня» (`N чек · ср. X`).
- [x] **Состояние смены/кассы** — карточка «Касса» (индикатор открытых смен → /sessions).
- [x] **Просроченные долги** — отдельная красная карточка (сумма + кол-во, по `dueDate < now`).
- [x] **Стоимость склада** — карточка `Σ(остаток × buyPrice)`.
- [x] **Разбивка оборота по оплатам** — stacked-бар «Способы оплаты (месяц)» нал/карта/долг/смешанно.
- [x] **Сравнение с прошлым периодом** — тренд ↑↓% на «Продажи сегодня» (vs вчера) и «за месяц» (vs прошлый месяц).

---

## 🏗️ Профессиональный уровень (общесистемное)

- [x] **Дробные количества** — `Product.quantity/minStock`, `SaleItem.quantity`, `StockIn.quantity` → `Decimal` (миграция `db push` в prod, каст Int→Decimal без потерь). По всему backend `parseInt`→`Number` + обёртка `Number()` вокруг арифметики/сравнений (иначе Decimal-как-строка ломает суммы и сравнения). Фронт: поля `step="any"`, POS-клавиатура без округления, сравнения остатков через `Number()`. ⚠️ Поднят таймаут транзакций (`transactionOptions` maxWait 15с/timeout 30с) — на медленной БД Railway транзакции иначе падали. ✅ вживую: приход 2.5 и продажа 1.5 шт работают.
- [ ] **Транзакции для всех мультитабличных операций** (продажи, склад, долги) — провести аудит, обернуть оставшиеся.
- [x] **Единый язык ошибок** — русифицированы все backend-сообщения: `auth` (сервис/контроллер/middleware), `sales`, `products`, `categories`, `clients`, `users`, `barcode`, `upload`, `stockin`. (Остались только нефункциональные комментарии в коде.)
- [ ] **Тесты** — сейчас их нет. Добавить хотя бы на критическую логику (confirmCart, stock-in, аналитика).
- [x] **Code-splitting фронта** — все страницы через `React.lazy` + `Suspense`. Главный бандл 2073 кБ → 263 кБ (gzip 85 кБ), варнинг исчез.
- [ ] **Раздельный учёт оплат** — модель `Payment[]` (CASH+CARD сейчас пишется одним `paymentType`), сдача нигде не сохраняется.
- [x] **Аудит чувствительных мутаций** — `audit()` добавлен на оформление продажи (`SALE_CONFIRM`), отмену (`SALE_CANCEL`), возврат (`SALE_RETURN`), удаление (`SALE_DELETE`), приход (`STOCK_IN`), правку/удаление прихода (`STOCK_EDIT`/`STOCK_DELETE`). ✅ вживую видно в журнале.

---

_Источник ревью: чтение кода + живые запросы к API (login → analytics → sales). Сгенерировано в сессии 2026-06-11._
